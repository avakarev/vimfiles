# avakarev's vim bundle

## Installation

Download minpac package manager and other deps as git submodules:

    make init

Update minpac to latest version:

    make update

Make symlinks to home directory:

    make link
